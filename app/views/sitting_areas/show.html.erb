<div class="container">
  <% Category.all.each do |category| %>
    <%= link_to new_sitting_area_order_path(@sitting_area, category_id: category.id), class: "d-block btn btn-primary mb-1" do %>
      <span><%= category.name %></span>
    <% end %>
  <% end %>
</div>


<div class = "container" style="margin-top: 10px;">
  <% Category.all.each do |category2| %>
  <%= link_to new_sitting_area_order_path(@sitting_area, category_id: category2.id) do %>
    <span><%= category2.name %></span>
    <% end %>
  <% end %>

  <div class = "container" style="margin-top: 10px;">
  <% Category.all.each do |category3| %>
  <%= link_to new_sitting_area_order_path(@sitting_area, category_id: category3.id) do %>
    <span><%= category3.name %></span>
    <% end %>
  <% end %>
  </div>

  <div class = "container" style="margin-top: 10px;">
  <%= link_to restaurants_path(@sitting_area) do%>
    <button type="button" class="btn btn-warning">Go to Condiments</button>
  <% end %>
  </div>

  <div class = "container" style="margin-top: 10px;">
  <%= link_to restaurants_path(@sitting_area) do%>
    <button type="button" class="btn btn-info">Go to Serviettes</button>
  <% end %>
  </div>

    <div class = "container" style="margin-top: 10px;">
  <%= link_to restaurants_path(@sitting_area) do%>
    <button type="button" class="btn btn-success">Go to verres</button>
  <% end %>
  </div>

    <div class = "container" style="margin-top: 10px;">
  <%= link_to restaurants_path(@sitting_area) do%>
    <button type="button" class="btn btn-outline-secondary">Custom Orders</button>
  <% end %>
  </div>

</div>
