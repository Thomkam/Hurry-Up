<div class= "container">
  <p>Vos urgences a gerer</p>
  <div class="">
    <%@sitting_areas.each do |table| %>
      <div>
      <p>table <%=table.sitting_area_number%></p>
        <div>
          <%table.orders.each do |order|%>
          <div class="d-flex justify-content-between" style="border: 1px solid">
            <p><%=order.item.name%> x<%=order.quantity%></p>
            <p>time</p>
            <div>
              <button>v</button>
              <button>x</button>
            </div>
            </div>
          <% end %>
        </div>
      </div>
      <%# all check button%>
      <div>
      <p>All</p>
      <button>v</button>
      <button>x</button>
      </div>
    <% end %>
  </div>
</div>
